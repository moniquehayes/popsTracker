import React from "react";

const Calendar = () => {
// can use state to load in the dates that are booked.
// will then need to reflect them on the calendar side. 

/**
 * OR I could assign each caretaker a certain color. and if they have a weekend, that weekend background color will be reflected 
 */

return (
<div>

</div>
)
};

export default Calendar;